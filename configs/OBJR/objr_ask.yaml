_BASE_: "../SA_base.yaml"
SEED: 0
DEVICE: 'cuda'
DATA:
  TYPE: 'objr' # ['clevr', 'CLEVR', 'ptr', 'PTR', 'msn', 'MSN']
  IMG_SIZE: 64
  NUM_WORKERS: 30
OUTPUT:
  DIR: './output_dir/objr_ask/'
TRAIN:
  OPTIMIZER: 'adam'
MODEL:
  SLOT:
    ATTN_SMOOTH: "normed_softmax"