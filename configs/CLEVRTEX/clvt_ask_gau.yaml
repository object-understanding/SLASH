_BASE_: "../SA_base.yaml"
SEED: 0
DEVICE: 'cuda'
DATA:
  TYPE: 'clevrtex' 
  IMG_SIZE: 128
  NUM_WORKERS: 30
OUTPUT:
  DIR: './output_dir/clvt_ask_gau/'
TRAIN:
  OPTIMIZER: 'adam'
MODEL:
  SLOT:
    NUM: 11
    ATTN_SMOOTH: "gaussian"
    ATTN_SMOOTH_GAU_MIN: 0.1
    ATTN_SMOOTH_GAU_MAX: 2.0